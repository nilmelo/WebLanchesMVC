@model Lunch

<div class="col-sm-4 col-lg-4 col-md-4">
	<div class="thumbnail">
		<img src="@Model.ImageURL" class="img-rounded" alt=""/>
	</div>
	<div class="caption">
		<h3 class="pull-righ">@Model.Price.ToString("c")</h3>
		<h3>@Model.Name</h3>
		<p>@Model.DescriptionShort</p>
	</div>
</div>
